export enum EncounterMessages{
    // Lifecycle
    INIT = 'encounter:init',
    JOIN = 'encounter:join',
    LEAVE = 'encounter:leave',
    END = 'encounter:end',
    // Data sync
    COMMAND = 'encounter:command', 
}